build:
  docker:
    web: nba-stats-recommender/backend/Dockerfile
release:
  command:
    - python nba-stats-recommender/backend/manage.py migrate
run:
  web: gunicorn nba-stats-recommender.backend.wsgi:application --bind 0.0.0.0:$PORT